// src/config.js
// 全局常量配置。这里的值是"游戏的规则"，调它们就能改玩法。
// 命名约束：所有常量大写 + 下划线，导出时用 export const。

// ─── 游戏规则 ────────────────────────────────────────
/** 底部槽位数量。槽满即失败。 */
export const SLOT_CAPACITY = 7;

/** 凑齐几张相同图案就消除。经典玩法 = 3。 */
export const MATCH_COUNT = 3;

// ─── 视觉常量 ────────────────────────────────────────
/** 单张牌的边长（像素）。手机 / 电脑都用这个尺寸。 */
export const TILE_SIZE = 60;

/** 牌之间的间距（像素）。 */
export const TILE_GAP = 4;

// ─── 调试 ────────────────────────────────────────────
/** 是否在控制台打印调试信息。发布前改 false。 */
export const DEBUG = true;

// ─── 关卡 ────────────────────────────────────────────
/** 默认进入的关卡 id（阶段 1 只有一关）。 */
export const DEFAULT_LEVEL_ID = 'level-01';

/** 阶段 1 的图案池（emoji 占位）。阶段 2 会换成图标资源。 */
export const SYMBOL_POOL = ['🐑', '🐱', '🐶', '🐰', '🐼', '🐯', '🐸', '🐵'];
