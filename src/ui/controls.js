// src/ui/controls.js
// 交互层：监听点击 / 键盘事件，改 state，再触发 render。
// 阶段 1.6 实现。
// 核心原则：所有"改 state"的代码只能从这里出发，别处不允许写 state.xxx = yyy。

/**
 * 在指定容器上绑定所有事件监听。
 * @param {HTMLElement} rootEl
 */
export function bindControls(rootEl) {
  // TODO 阶段 1.6
}
